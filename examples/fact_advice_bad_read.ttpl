(lambda (thunk)
  ((file "around.ttpl") 
   ((file "call.ttpl") 'fact)
   (lambda (proceed)
     (lambda (y) 
       (let ([result (proceed y)])
         (seq (write "y" (read "more input"))
              (seq (write "result" result)
                   result)))))
   thunk))
